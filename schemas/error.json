{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "$id": "error",
    "title": "Error",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Human readable name for the error."
        },
        "message": {
            "type": "string",
            "description": "An object with fields name as keys and associated message as values."
        },
        "fields": {
            "type": "object",
            "description": "Human readable message, describing the error."
        }
    },
    "required": ["name"]
}
